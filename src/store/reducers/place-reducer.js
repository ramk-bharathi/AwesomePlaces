const initialState = {
    places: [],
    selectedPlace: null
};
const placeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default placeReducer;